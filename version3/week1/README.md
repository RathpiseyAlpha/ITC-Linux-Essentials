# Linux Essentials Weekly Report — Week 1 (Lighter Version)

- **Student Name:** [Your Full Name]
- **Student ID:** [Your Student ID Number]
- **Class/Group:** [Your Class Section]

---

## 💻 Session 1: Linux CLI Introduction & Getting Help

### Exercises:
1.  **Navigate to `/usr/share/doc` and list contents:**
    *   Command used:
    *   List three items you found inside this directory:

2.  **Return to Home in a single command:**
    *   Command used:

3.  **Long listing of Home directory showing hidden files:**
    *   Command used:
    *   List all shell-related configuration files you observed:

4.  **`whatis` commands output (`whatis_summary.txt`):**
    *   What command did you run to redirect all outputs into `whatis_summary.txt`?
    *   Paste the content of your `whatis_summary.txt` file here:
        ```text
        [Paste content here]
        ```

5.  **Command History output (`history_list.txt`):**
    *   Paste the content of your `history_list.txt` file here:
        ```text
        [Paste content here]
        ```

---

## 📂 Session 2: Directory and File Management

### Exercises:
1.  **Create sandbox/archive/ and sandbox/workspace/:**
    *   Command(s) used:

2.  **Create 5 text files in workspace/:**
    *   Command used:

3.  **Copy all test files to archive/ in a single command:**
    *   Command used:

4.  **Move all sample files to archive/ in a single command:**
    *   Command used:

5.  **Verify directories and delete workspace recursively:**
    *   Command used to list directories:
    *   Command used to delete workspace recursively:

---

## 🔗 Session 3: Symbolic Shortcuts and Searching Files

### Exercises:
1.  **Create important.dat in sandbox/archive/:**
    *   Command:

2.  **Create a symbolic link to important.dat in the parent directory named shortcut.lnk:**
    *   Command:

3.  **Locate executable binary path of `tar` and `zip` using `which`:**
    *   `tar` Path:
    *   `zip` Path:

4.  **Search all `.conf` files inside `/etc` using `find`:**
    *   Command used:

5.  **Delete original `important.dat` and open `shortcut.lnk`:**
    *   What happens when you try to view the contents of the shortcut?
    *   Explain why this behavior occurs:

---

## 🧩 Week 1 Challenge Scenario: "Audit and Server Structure Reorganization"

Please answer the following questions based on the scenario challenge:

1.  **Audit Results:**
    *   Identify the largest file in `/var/tmp/apex_audit/` and its size:
    *   Paste the content of your generated `audit_report.txt` here:
        ```text
        [Paste content here]
        ```

2.  **Apollo Organization:**
    *   What wildcard command did you use to move the config files?
    *   What wildcard command did you use to move the script files?
    *   What wildcard command did you use to clean up the drafts and temp files in a single step?
    *   Explain how the symbolic link `quick_run.sh` behaves compared to the original script:

### Screenshot proof of the Scenario:
<!-- SCREENSHOT REQUIREMENT: Insert your screenshot showing your terminal workspace as you ran the audit command. -->
![Audit Scenario Screenshot](./images/audit_scenario.png)

<!-- SCREENSHOT REQUIREMENT: Insert your screenshot showing the contents of the apollo/ directory and links. -->
![Apollo Organization Screenshot](./images/apollo_organization.png)
