# Linux Essentials Weekly Report — Week 2 (Lighter Version)

- **Student Name:** [Your Full Name]
- **Student ID:** [Your Student ID Number]
- **Class/Group:** [Your Class Section]

---

## 💻 Session 4: I/O Streams, Redirection & Pipelines

### Exercises:
1.  **Write `os_list.txt` directly from console using redirection:**
    *   Command used:
    *   Output of `cat os_list.txt`:
        ```text
        [Paste content here]
        ```

2.  **Append `RedHat` and `Alpine`:**
    *   Commands used:

3.  **Extract lines 15 to 20 of `/etc/services`:**
    *   Pipeline command:

4.  **Count lines containing 'nologin' in `/etc/passwd`:**
    *   Command used:
    *   Result:

---

## 📂 Session 5: Package Management & Archiving

### Exercises:
1.  **Search and install package:**
    *   Command to search:
    *   Command to install:

2.  **Create backup.tar.gz:**
    *   Command used:

3.  **Extract backup.tar.gz:**
    *   Command used:

4.  **Create and compare sizes:**
    *   Command used to create ZIP:
    *   Size of `backup.tar.gz`:
    *   Size of `backup.zip`:

---

## 🔗 Session 6: Basic Shell Scripting

### Exercises:
1.  **Script implementation (`check_file.sh`):**
    *   Paste the complete contents of your `check_file.sh` script here:
        ```bash
        [Paste script code here]
        ```

2.  **Make script executable:**
    *   Command used:

3.  **Execute script for an existing file:**
    *   Filename entered:
    *   Terminal output:
        ```text
        [Paste output here]
        ```

4.  **Execute script for a non-existing file:**
    *   Filename entered:
    *   Terminal output:
        ```text
        [Paste output here]
        ```

---

## 🧩 Week 2 Challenge Scenario: "Automated Log Rotation & Audit Script"

Please answer the following questions based on the scenario challenge:

1.  **Log File Audit:**
    *   Pipeline command to count total requests:
    *   Total requests count:
    *   Pipeline command to filter failed attempts (401 or 404):
    *   Paste first 5 lines of `failed_attempts.txt` here:
        ```text
        [Paste here]
        ```
    *   What sensitive files did the intruder attempt to access? (Paste matching lines from `attack_signatures.txt`):
        ```text
        [Paste here]
        ```

2.  **Audit Script implementation:**
    *   Paste the complete contents of your `auto_audit.sh` script here:
        ```bash
        [Paste script code here]
        ```

3.  **Script execution:**
    *   Output of running `./auto_audit.sh`:
        ```text
        [Paste here]
        ```
    *   Paste the contents of the generated `deploy.log` file here:
        ```text
        [Paste here]
        ```

### Screenshot proof of the Scenario:
<!-- SCREENSHOT REQUIREMENT: Insert your screenshot showing your log auditing process in terminal. -->
![Log Analysis Screenshot](./images/log_analysis.png)

<!-- SCREENSHOT REQUIREMENT: Insert your screenshot showing the successful output and execution of auto_audit.sh. -->
![Script Execution Screenshot](./images/script_execution.png)
