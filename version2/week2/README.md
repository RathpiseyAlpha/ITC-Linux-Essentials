# Linux Essentials Weekly Report — Week 2

- **Student Name:** [Your Full Name]
- **Student ID:** [Your Student ID Number]
- **Class/Group:** [Your Class Section]

---

## 💻 Session 4: I/O Streams, Redirection & Archiving

### Exercises:
1.  **Write `os_list.txt` directly from console using redirection:**
    *   Command used:
    *   Output of `cat os_list.txt`:
        ```text
        [Paste content here]
        ```

2.  **Append `RedHat` and `Alpine`:**
    *   Commands used:

3.  **Extract lines 20 to 25 of `/etc/services`:**
    *   Pipeline command:

4.  **Create compressed archive `backup.tar.gz`:**
    *   Command used to create folder & copy file:
    *   Command used to create tarball:

5.  **List `backup.tar.gz` contents:**
    *   Command used:
    *   Paste listed output here:
        ```text
        [Paste here]
        ```

---

## 📂 Session 5: Basic Shell Scripting

### Exercises:
1.  **Script implementation (`check_file.sh`):**
    *   Paste the complete contents of your `check_file.sh` script here:
        ```bash
        [Paste script code here]
        ```

2.  **Make script executable:**
    *   Command used:

3.  **Execute script for an existing file:**
    *   Filename entered:
    *   Terminal output:
        ```text
        [Paste output here]
        ```

4.  **Execute script for a non-existing file:**
    *   Filename entered:
    *   Terminal output:
        ```text
        [Paste output here]
        ```

5.  **What is the exit status code of your script in both test runs?**
    *   Command used to inspect exit status:
    *   Exit status for existing file run:
    *   Exit status for non-existing file run:

---

## 🔗 Session 6: Software Package Management & Compilation

### Exercises:
1.  **Search and install `sl` package:**
    *   Command to search:
    *   Command to install:

2.  **List files installed by `sl`:**
    *   Command used:
    *   Identify execution path of `sl`:

3.  **Makefile project compilation:**
    *   Command used to compile project using `make`:
    *   Output of executing the binary `./hello`:
        ```text
        [Paste here]
        ```
    *   Command used to clean workspace:

4.  **Create and compare archive sizes:**
    *   Command used to create `backup.tar`:
    *   Command used to create `backup.tar.gz`:
    *   Content of `size_comparison.txt` (output of `ls -lh`):
        ```text
        [Paste here]
        ```

---

## 🧩 Week 2 Challenge Scenario: "Automated Log Rotation & App Builder Script"

Please answer the following questions based on the scenario challenge:

1.  **Log File Audit:**
    *   Pipeline command to count total requests:
    *   Total requests count:
    *   Pipeline command to filter failed attempts (401 or 404):
    *   Paste first 5 lines of `failed_attempts.txt` here:
        ```text
        [Paste here]
        ```
    *   What sensitive files did the intruder attempt to access? (Paste matching lines from `attack_signatures.txt`):
        ```text
        [Paste here]
        ```

2.  **Deployment Script implementation:**
    *   Paste the complete contents of your `auto_deploy.sh` script here:
        ```bash
        [Paste script code here]
        ```

3.  **Script execution:**
    *   Output of running `./auto_deploy.sh`:
        ```text
        [Paste here]
        ```
    *   Paste the contents of the generated `deploy.log` file here:
        ```text
        [Paste here]
        ```

### Screenshot proof of the Scenario:
<!-- SCREENSHOT REQUIREMENT: Insert your screenshot showing your log auditing process in terminal. -->
![Log Analysis Screenshot](./images/log_analysis.png)

<!-- SCREENSHOT REQUIREMENT: Insert your screenshot showing the successful output and execution of auto_deploy.sh. -->
![Script Execution Screenshot](./images/script_execution.png)
