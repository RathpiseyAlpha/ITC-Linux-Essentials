# Linux Essentials Weekly Report — Week 2

- **Student Name:** [Your Full Name]
- **Student ID:** [Your Student ID Number]
- **Class/Group:** [Your Class Section]

---

## 💻 Session 4: I/O Streams and Redirection

### Exercises:
1.  **Write `os_list.txt` directly from console using redirection:**
    *   Command used:
    *   Output of `cat os_list.txt`:
        ```text
        [Paste content here]
        ```

2.  **Append `RedHat` and `Alpine`:**
    *   Commands used:

3.  **Redirect stderr of invalid directory listing:**
    *   Command used:
    *   Content of `stderr_log.txt`:
        ```text
        [Paste content here]
        ```

4.  **Extract lines 20 to 25 of `/etc/services`:**
    *   Pipeline command:

5.  **Append line count of `/etc/services`:**
    *   Command used:
    *   Output in `services_range.txt`:
        ```text
        [Paste content here]
        ```

---

## 📂 Session 5: Text Processing & Filter Pipelines

### Exercises:
1.  **Search for "ssh" (case-insensitive) in `/etc/services`:**
    *   Command used:
    *   Paste first 3 lines of output from `port_services.txt`:
        ```text
        [Paste here]
        ```

2.  **Pipeline command to count and sort unique guest names in `guest_counts.txt`:**
    *   Command used:
    *   Content of `guest_counts.txt`:
        ```text
        [Paste content here]
        ```

---

## 🔗 Session 6: Software Package Management & Archiving

### Exercises:
1.  **Search and install `sl` package:**
    *   Command to search:
    *   Command to install:

2.  **List files installed by `sl`:**
    *   Command used:
    *   Identify execution path path:

3.  **Create plain and compressed tarball sizes comparison:**
    *   Command to create plain tarball:
    *   Command to create compressed tarball:
    *   Content of `size_comparison.txt`:
        ```text
        [Paste content here]
        ```
    *   Which archive is smaller, and why?

---

## 🧩 Week 2 Challenge Scenario: "Security Log Audit and Web App Deployment"

Please answer the following questions based on the scenario challenge:

1.  **Log File Audit:**
    *   Pipeline command to count total requests:
    *   Total requests count:
    *   Which URL targets were hit most frequently? (Paste content of `frequent_targets.txt`):
        ```text
        [Paste here]
        ```
    *   What sensitive files did the intruder attempt to access?

2.  **Web App Deployment:**
    *   Command to package `apollo_app/`:
    *   Command to install `apex-logger.deb`:
    *   Paste content of `deployment_status.txt`:
        ```text
        [Paste here]
        ```
    *   Paste metadata info from `logger_info.txt`:
        ```text
        [Paste here]
        ```

### Screenshot proof of the Scenario:
<!-- SCREENSHOT REQUIREMENT: Insert your screenshot showing your terminal workspace with the log processing command pipelines. -->
![Log Analysis Screenshot](./images/log_analysis.png)

<!-- SCREENSHOT REQUIREMENT: Insert your screenshot showing your clean folder structure containing apollo_backup.tar.gz and reports. -->
![Package Deploy Screenshot](./images/package_deploy.png)
