# Linux Essentials Lab 6 — Processes, Monitoring & Networking Report

- **Student Name:** [Your Full Name]
- **Student ID:** [Your Student ID Number]
- **Class/Group:** [Your Class Section]

---

## 📂 Part 1: Process Management

### Basic Exercises (1-6):
1.  **Initial jobs listing (`jobs_list.txt`):**
    *   Paste content of `jobs_list.txt`:
        ```text
        [Paste content here]
        ```

2.  **Job status description after suspending the sleep job:**
    *   What status is shown in the jobs list after pressing `Ctrl+Z`?
        *   Status:

3.  **Process termination commands:**
    *   What PIDs were assigned to the sleep jobs?
        *   Task 1 PID:
        *   Task 2 PID:
    *   Command(s) used to kill the processes:
        *   Command:

---

## 🔗 Part 2 & 3: Monitoring and Networking

### Basic Exercises (7-10):
4.  **Memory capacity metrics (`memory_status.txt`):**
    *   Paste content of `memory_status.txt`:
        ```text
        [Paste content here]
        ```

5.  **Disk space capacity metrics (`disk_status.txt`):**
    *   Paste content of `disk_status.txt`:
        ```text
        [Paste content here]
        ```

6.  **Local IP configuration parameters (`network_config.txt`):**
    *   Primary Network Interface name:
    *   IPv4 Address:
    *   Paste content of `network_config.txt`:
        ```text
        [Paste content here]
        ```

7.  **Loopback connection ping testing (`ping_localhost.txt`):**
    *   Paste content of `ping_localhost.txt`:
        ```text
        [Paste content here]
        ```

---

## 🧩 Lab Scenario: "Troubleshooting the Frozen Web Server" Challenge

Briefly describe how you diagnosed and recovered the staging server:

1.  **Rogue process details:**
    *   Rogue script process name:
    *   Rogue PID:
    *   How did you find it? (Command used):

2.  **Rogue process termination:**
    *   Command used to terminate the process:

3.  **Recovered system status details (`system_recovery.txt`):**
    *   Paste content of `system_recovery.txt`:
        ```text
        [Paste content here]
        ```

4.  **Port check results (`port_check.txt`):**
    *   Command used to check port 8080:
    *   Paste content of `port_check.txt`:
        ```text
        [Paste content here]
        ```
    *   Is port 8080 now free or still bound?

### Screenshot proof of the server recovery:
<!-- SCREENSHOT REQUIREMENT: Insert your screenshot showing your terminal workspace with output of ps (demonstrating that the rogue loop is gone). -->
![System Monitoring Scenario Screenshot](./images/system_monitoring.png)
