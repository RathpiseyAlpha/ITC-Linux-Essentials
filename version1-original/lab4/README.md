# Linux Essentials Lab 4 — Package Management & Archiving Report

- **Student Name:** [Your Full Name]
- **Student ID:** [Your Student ID Number]
- **Class/Group:** [Your Class Section]

---

## 📂 Part 1: Package Management

### Basic Exercises (1-4):
1.  **Command to search for the locomotive (`sl`) package:**
    *   Command:

2.  **Command to install `sl` package:**
    *   Command:

3.  **Command to list all files installed by the `sl` package:**
    *   Command:
    *   Where is the binary binary executable file placed on the filesystem (e.g. `/usr/games/` or `/usr/bin/`)?
        *   Path:

4.  **Difference between `apt-get` and `dpkg`:**
    *   Explanation:

---

## 🔗 Part 2: Archiving and Compression

### Basic Exercises (5-10):
5.  **Commands used to create `sandbox_deploy/` and files:**
    *   Command:

6.  **Commands to create `backup.tar` and `backup.tar.gz`:**
    *   plain tar Command:
    *   gzip-compressed tar Command:

7.  **Comparison output from `size_comparison.txt`:**
    *   Paste comparison file list and sizes here:
        ```text
        [Paste content here]
        ```
    *   Which file is smaller, and why?

8.  **Commands to zip and unzip `backup.zip` into `check_extract/`:**
    *   zip Command:
    *   unzip Command:

---

## 🧩 Lab Scenario: "Deploying and Archiving the Apex App" Challenge

Briefly describe how you completed the deployment and archiving task for Apex Systems:

1.  **Command to archive and compress `apollo_app/`:**
    *   Command:

2.  **Command to install the local package `apex-logger.deb`:**
    *   Command:

3.  **Output of the logger command execution (`deployment_status.txt`):**
    *   Paste content of `deployment_status.txt`:
        ```text
        [Paste content here]
        ```

4.  **Metadata summary of `apex-logger` package (`logger_info.txt`):**
    *   Paste contents of `logger_info.txt`:
        ```text
        [Paste content here]
        ```

### Screenshot proof of the final state after cleanup:
<!-- SCREENSHOT REQUIREMENT: Insert your screenshot showing the contents of your lab4/ directory, confirming that apollo_app/ and apex-logger.deb are removed, but apollo_backup.tar.gz remains. -->
![Package Deploy Scenario Screenshot](./images/package_deploy.png)
